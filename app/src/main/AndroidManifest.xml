<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bitlogictechnologies.danwedsphilo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/FullTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Order"/>
        <activity android:name=".Functions"/>
        <activity android:name=".ServiceOrder"/>
        <activity android:name=".PhotoShoot"/>
        <activity android:name=".Reception"/>
        <activity android:name=".Protocol"/>
        <activity android:name=".Acknow"/>
        <activity android:name=".Appre"/>
        <activity android:name=".Hymn"/>
        <activity android:name=".Hymn2"/>
    </application>

</manifest>